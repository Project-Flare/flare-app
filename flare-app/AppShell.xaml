<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="flare_app.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:flare_app"
    Shell.FlyoutBehavior="Disabled"
    Shell.NavBarIsVisible="False"
    Title="flare_app">


       <ShellContent
        Title="Login"
        ContentTemplate="{DataTemplate local:LoginPage}"
        Route="LoginPage" />

        <ShellContent
        Title="Registration"
        ContentTemplate="{DataTemplate local:RegistrationPage}"
        Route="RegistrationPage" />

        <ShellContent
        Title="User list"
        ContentTemplate="{DataTemplate local:UserListPage}"
        Route="UserListPage" />

    <ShellContent
        Title="Discovery"
        ContentTemplate="{DataTemplate local:DiscoveryPage}"
        Route="DiscoveryPage" />


</Shell>
